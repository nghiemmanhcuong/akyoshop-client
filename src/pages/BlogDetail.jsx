import React from 'react';
import PropTypes from 'prop-types';
import Helmet from '../components/Helmet';

const BlogDetail = (props) => {
    return <Helmet>blog detail</Helmet>;
};

BlogDetail.propTypes = {};

export default BlogDetail;
